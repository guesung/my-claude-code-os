# 리뷰 지식을 먹고 자라는 구현 OS

> 4주 동안 만들 나만의 클로드 코드 OS. [docs/ideas.md](./docs/ideas.md) 1번(기능 구현 OS)을 축으로 하고,
> **"과거 코드리뷰에서 받은 지적을 자산화해 다음 구현에 먹인다"** 를 차별화 축으로 삼는다.

## 1. 한 줄 정의

**설계는 사람이 확정하고, 구현부터 Draft PR까지는 AI가 한다.**
단 AI는 **리뷰 원장(ledger)** 을 기준으로 스스로를 검증하며,
사람에게 지적받은 것은 원장에 적립되어 다음 사이클의 검증 기준이 된다.

> **변경 이력** — 초기 정의는 "설계는 사람이 **하고**"였다. [docs/ideas.md](./docs/ideas.md) 1.3.4를 반영해
> AI가 설계 초안과 "막힐 지점"을 낼 수 있도록 열되, **사람의 꼼꼼한 검토를 통과해야만 확정**되는 형태로 바꿨다.
> 설계 권한이 사람에게 있다는 원칙은 그대로다. 바뀐 건 AI가 설계 단계에 **참여하는 방식**이다.

## 2. 왜 이 주제인가

강의 온보딩 문서가 제시한 OS의 조건에 비춘 판단.

| 조건 | 이 OS에서 충족되는 방식 |
|---|---|
| 자동 피드백 신호가 있는가 | 타입체크 · 빌드 · 컨벤션 위반 · 스크린샷 · 원장 항목 위반 · QA 항목별 PASS/FAIL |
| 사람의 역할이 명확한가 | **설계 검토**와 **최종 승인**만. 그 사이는 AI |
| 주차별로 자동화 수준을 올릴 수 있는가 | 사람 개입 횟수를 5회 → 1회로 줄여가는 궤적이 곧 커리큘럼 |
| 조별 리뷰에서 보여줄 게 나오는가 | 매주 실제 PR 1개 + 재발률 숫자 |

핵심은 온보딩 문서의 질문 **"우리가 가진 지식을 AI에게 어떻게 전달하지?"** 에 정면으로 답한다는 점이다.
컨벤션 문서를 컨텍스트에 붙이는 건 누구나 한다. 이 OS는 **사람이 실제로 지적한 것**을 소스로 삼는다.

## 3. 골격

온보딩 문서의 공통 골격(`의도 공유 → 실행 → 피드백 → ↻`)에 대응시킨 형태.

```
[사람]  설계 확정 ─────────────────────────────────────► 최종 승인 → PR 공유
          ▲  │                                                ▲
    검토·컨펌 │                                                │
          │  ▼                                                │
[AI]  설계 초안 · "막힐 지점" 지적                              │
             │                                                │
             ▼                                                │
          구현 ─→ 셀프 리뷰 ─→ 리팩토링 ─→ 교차 컨펌 ─→ 셀프 QA ─┘
                    ↑                         │          │
                    └────── ↻ (상한 3회) ──────┘          └─→ 자동 불가분은
                    │                                        재현 방법 안내 → 수동 QA
              [리뷰 원장] ←── 사람이 새로 지적한 것 적립
                    │
                    └──→ [검사기] 원장 항목을 실행 가능한 검사로 승격
```

- **의도 공유** = 설계서 (AI가 초안·지적, 사람이 검토·확정)
- **실행** = 구현 → 셀프 리뷰 → 리팩토링 → 교차 컨펌 → 셀프 QA
  - 리팩토링은 두 갈래: 원장·컨벤션 **위반 수정**(필수) + [Frontend Fundamentals](https://github.com/toss/frontend-fundamentals) 기준 **개선 제안**(사람이 선택)
- **피드백** = 사람의 최종 검토. 여기서 나온 지적이 원장으로 되돌아감 ← **이 화살표가 이 OS의 정체성**

되먹임 화살표가 없으면 이건 그냥 자동화지 OS가 아니다. 루프를 돌수록 판정 기준이 두꺼워지는 것이 핵심이다.

## 4. 구성 요소

### 4.1 리뷰 원장 (Ledger)

이 OS의 심장. 한 항목 = 한 개의 "다시는 지적받지 않겠다"는 규칙.

```yaml
- id: L-001
  지적: useCallback을 습관적으로 붙임
  출처: PR #482 리뷰 코멘트
  검증법: 변경분에서 useCallback/useMemo 사용처 grep
  재발: 2회
```

- 검증 가능한 형태로 적는 것이 관건. "코드를 깔끔하게"는 원장 항목이 될 수 없다.
- 재발 횟수가 곧 4주차의 채점 지표.

### 4.2 검사기 — 원장의 실행 가능한 형태

원장 항목을 문서로만 두면 **AI가 잘 봐주기를 기대**해야 한다. 검사기로 승격시키면 기계가 막는다.

| 원장 항목 | 승격된 검사 |
|---|---|
| L-001 useCallback 습관적 사용 | 변경분에 `useCallback`/`useMemo` 등장 여부 grep |

원장이 쌓일수록 검사기가 자동으로 두꺼워진다. "재발 0"을 희망이 아니라 보장으로 바꾸는 지점이고,
4주차 하네스 개념과 그대로 맞물린다.

### 4.3 설계 단계 — 초안은 AI, 확정은 사람

1절의 "AI가 설계에 참여하되 사람의 검토를 통과해야만 확정"을 실제 절차로 옮긴 것이 `/os:design`이다. 근거와 결정 이력은 [docs/design-stage.md](./docs/design-stage.md).

```
/os:work-card  씨앗 (한 줄 목표 · 막힐 지점 초벌)   ← 슬랙·노션만 본다
      ▼
/os:design     레포를 열어 AS-IS · TO-BE · 변경 지점(file:line) · 동작 명세
               조건부 Mermaid → 막힐 지점 해소 → ★ 사람 승인 → 확정 도장
      ▼
/os:implement-loop  도장이 없으면 시작하지 않는다
```

- **다이어그램은 조건부다.** 분기 2개 이상 · 왕복 2회 이상 · 상태 3개 이상 · 새 파일 3개 이상일 때만 그린다. 미해당이면 "해당 없음 + 이유"를 남긴다 — 안 그린 것이 판단인지 누락인지 구별되어야 한다.
- **확정 도장은 기계적 강제가 아니다.** AI도 같은 API로 쓸 수 있다. 규칙으로 막아두고, 검사기 승격 후보로 남겨둔 상태다(4.2와 같은 경로).

### 4.4 역할 분리와 교차 검토

| 역할 | 하는 일 | 왜 분리하나 |
|---|---|---|
| 구현자 | 설계서 → 코드 | |
| 리뷰어 | 원장 + 컨벤션 기준으로 판정 | **자기 코드를 자기가 승인하면 무조건 통과시킨다.** 별도 컨텍스트여야 함 |
| 교차 컨펌 | 리팩토링 결과를 다른 AI가 재검토 | 한 번의 검토는 놓친다 |
| 검증자 | 빌드 · 타입 · 실제 화면 동작 | 코드를 읽는 것과 돌려보는 것은 다른 일 |

교차 컨펌에는 함정이 있다. **같은 모델이면 맹점도 같아서** 두 번 봐도 같은 걸 놓친다. 두 가지로 보정한다.

- **역할을 비대칭으로** — 두 번째 검토자에게 "통과시켜도 되는지"가 아니라 **"이 PR을 반려시킬 근거를 찾아라"** 를 시킨다. 입증 책임을 뒤집는다.
- **의도를 숨기고 diff만 주기** — 설계서 없이 코드만 보고 의도가 읽히면, 그 자체가 가독성 테스트가 된다.

### 4.5 셀프 QA

```
기획서 → QA 명세서 작성 → 자동 QA (Playwright MCP) → 자동 불가분은 재현 방법 안내 → 수동 QA
```

전부 자동화하려 들지 않는 것이 요점이다. 자동으로 못 하는 항목은 **재현 방법을 사람에게 넘겨** 수동 QA로 처리한다.
QA 항목은 노션 "QA 항목" DB에 작업별로 쌓인다(`/os:self-qa`). 자동/수동 경계가 데이터로 남아야 "검증 안 한 항목"이 사라진다.
"자동화 못 하니 검증 안 함"이 되지 않게 경계를 명시적으로 남긴다.

### 4.6 PR 분할 기준

작은 단위로 브랜치·PR을 나눈다. 기준을 명확한 숫자로 세운다.

- 변경 파일 수 기준 (assets·자동생성 파일 제외)
- 기준 초과 시 분할을 제안하고, 사람이 결정

> 파일 개수만으로는 한 파일 500줄 변경을 못 잡는다. 변경 라인 수를 함께 보거나,
> "PR을 한 문장으로 요약했을 때 '그리고'가 들어가면 쪼갠다" 같은 의도 단위 기준을 얹는 안을 검토 중. → 6절

### 4.7 작업 카드 단일화 (SSOT)

작업 메모를 md 파일로 만들어 gitignore에 넣으면 **레거시가 되고 나중에 찾기 어렵다.**
노션을 단일 진실 공급원(SSOT)으로 두고, 작업 카드를 거기로 모은다.
팀 작업 카드(`1파트 작업리스트`)는 팀이 보는 상태판이라 **AI는 읽기만** 하고, 상황 공유는 개인 `업무 로그` DB 페이지에 **설계서 · TDL · QA 체크리스트 · 논의 필요 · 루프 로그**로 쌓는다. 둘은 슬랙 스레드 URL로 묶인다. → [docs/orchestrator-plan.md](./docs/orchestrator-plan.md) 2절

### 4.8 문서 유지보수 규칙

이 문서와 원장은 방치하면 현실과 어긋난다. 두 가지 장치를 둔다.

- 문서를 업데이트하면 **사람에게 검토받는다**
- 일정 주기로 사람이 직접 문서를 점검한다

### 4.9 재사용할 기존 자산

이미 갖고 있는 스킬들. 새로 만드는 게 아니라 **하나의 루프로 묶는 것**이 이 강의의 과제.

`/convention-review` · `/full-review` · `/pr` · `/ui-verify` · `/review-feedback-log` · `/spec-build`

전부 전역 플러그인(`~/.claude/skills/guesung/`)에 있다. 이 레포로 복사·symlink 하지 않고 **이름으로 참조**한다. 경계와 근거는 [docs/orchestrator-plan.md](./docs/orchestrator-plan.md) 0절.

## 5. 4주 로드맵

> 실제 구축 순서·파일 구조는 [docs/orchestrator-plan.md](./docs/orchestrator-plan.md).

각 주차의 성공 기준은 "사람 개입 횟수가 줄었는가".

| 주차 | 배울 개념 | 얹을 것 | 사람 개입 |
|---|---|---|---|
| 1 | 스킬 · 컨텍스트 | 설계서 → 구현 스킬 1개. 원장 v0 (수기 10항목) | 설계 검토 + 매 단계 호출 |
| 2 | 에이전트 | 구현자 / 리뷰어 분리. 리뷰어는 원장으로만 판정. 검사기 v0 | 리뷰 결과 승인 |
| 3 | 오케스트레이터 · 루프 | 구현→리뷰→리팩토링→교차 컨펌 루프 (상한 3회). 셀프 QA 결합 | 시작 + 최종 확인 |
| 4 | 하네스 · 평가 | 재발률 채점. Draft PR까지 자동. 실패는 원장에 적립 | 시작만 |

버그 픽스 OS([docs/ideas.md](./docs/ideas.md) 3번)는 폐기하지 않고, 3주차에 **같은 골격을 재사용하는 두 번째 진입점**으로 검토한다.
원장과 리뷰어를 그대로 쓰고 앞단만 "재현"으로 바꾸면 된다.

## 6. 열린 결정 사항

- [x] 개인 프로젝트도 이 OS로 커버할 것인가 → **아니오. 이 OS의 범위는 회사 업무로 한정한다.** 이 OS의 심장인 되먹임 화살표(3절)의 연료는 회사 코드리뷰에서 사람이 남긴 지적인데, 개인 프로젝트에는 리뷰어가 없어 원장이 자라지 않는다 — 루프 없는 실행은 그냥 자동화지 OS가 아니다. 파이프라인도 슬랙 스레드·팀 작업리스트·테섭 배포 등 회사 인프라에 결합되어 있고, 개인 프로젝트가 필요로 하는 앞단(기획·디자인의 발산)은 범용 스킬(brainstorming 등)의 몫이다. 따라서 `/os:design`·`/os:implement-loop`는 대상 레포가 cashwalk 밖이면 **시작 자체를 거부한다**(로컬 설계서 파일로 도는 개발·검증용 실행만 예외). 판정 구간만 건너뛰는 얇은 경로를 돌려주는 것은 "범위 밖" 결정과 어긋나는 어정쩡한 상태라 제거했다 — 이 판정의 역할은 실수로 개인 레포에서 루프를 돌렸을 때 팀 원장을 들이대지 않게 막는 가드레일이다.
- [x] 리뷰 원장의 소스 → **노션 "코드 리뷰 문서화 DB"(적립층) + 레포 `ledger.yaml`(실행층)** 2층 구조. [docs/orchestrator-plan.md](./docs/orchestrator-plan.md) 2절
- [ ] 실습 대상 코드베이스를 무엇으로 할 것인가 (실제 업무 레포 / 별도 샘플)
- [x] 설계서의 형식과 저장 위치 → **노션 개인 업무 로그 페이지 `## 설계서` 섹션 단일.** 6항목(목표·AS-IS·TO-BE·변경 지점·동작 명세·범위 밖) + 조건부 Mermaid. [docs/design-stage.md](./docs/design-stage.md)
- [ ] PR 분할 기준의 구체적 임계값 — 파일 수 몇 개? 라인 수를 함께 볼 것인가?
- [ ] 문서 정기 점검의 주기

## 7. OS 범위 밖 과제

이 OS의 구성 요소는 아니지만, 실습 환경이 굴러가려면 필요한 것들. ([docs/ideas.md](./docs/ideas.md) 2번)

- **스킬 동기화** — 회사 PC와 개인 노트북의 스킬이 어긋나지 않게. PC 실행 시 pull, 업데이트 후 push
- **컨벤션 격리** — 회사 프로젝트 컨벤션과 개인 프로젝트 컨벤션이 서로 새지 않게 분리
