# 리뷰 지식을 먹고 자라는 구현 OS

> 나만의 클로드 코드 OS 청사진. 이 문서가 **단일 진입점**이다.
> 탐색 과정은 [docs/ideas.md](./docs/ideas.md), 설계 초안은 [docs/os-design.md](./docs/os-design.md),
> 미채택 후보는 [docs/os-alternatives.md](./docs/os-alternatives.md)에 있다.

## 한 줄 정의

**설계는 사람이 하고, 구현부터 Draft PR까지는 AI가 한다.**
단 AI는 **리뷰 원장(ledger)** 을 기준으로 스스로를 검증하며,
사람에게 새로 지적받은 것은 원장에 적립되어 다음 사이클의 검증 기준이 된다.

## 문제 의식

컨벤션 문서를 컨텍스트에 붙이는 건 누구나 한다. 그런데도 같은 지적을 반복해서 받는다.
이유는 컨벤션 문서가 **일반론**이기 때문이다. "코드를 깔끔하게 짜라"는 문장은 검증할 수 없다.

이 OS는 소스를 바꾼다. 일반론이 아니라 **사람이 나에게 실제로 지적한 것**을 원장에 쌓고,
그 원장을 다음 구현의 판정 기준으로 되먹인다. 지적은 구체적이고, 구체적인 것은 검증 가능하다.

## 설계 원칙

| 원칙 | 의미 |
|---|---|
| 사람은 판단만 한다 | 설계와 최종 승인은 위임하지 않는다. 그 사이는 AI |
| 검증 가능한 것만 원장에 넣는다 | grep·빌드·스크린샷으로 판정 못 하면 항목이 아니다 |
| 자기 코드를 자기가 승인하지 않는다 | 구현자와 리뷰어는 반드시 별도 컨텍스트 |
| 실패는 지우지 않고 적립한다 | 놓친 지적은 그 자체가 다음 주차의 원장 항목 |

## 골격

```
[사람]  설계                                    최종 승인 → PR 공유
          │                                        ↑
          ▼                                        │
[AI]   구현 ──→ 셀프 리뷰 ──→ 수정 ──→ 시각 검증 ──→ Draft PR
                  ↑    (상한 3회)  │
                  └───────↻───────┘
                  │
              [리뷰 원장]  ←── 사람이 새로 지적한 것 적립
```

- **의도 공유** = 설계서 (사람이 작성, AI는 읽기만)
- **실행** = 구현 → 셀프 리뷰 → 수정 루프
- **피드백** = 사람의 최종 검토. 여기서 나온 지적이 원장으로 되돌아감 ← **이 화살표가 이 OS의 정체성**

되먹임 화살표가 없으면 이건 그냥 자동화지 OS가 아니다. 루프를 돌수록 판정 기준이 두꺼워지는 것이 핵심이다.

## 구성 요소

### 리뷰 원장 (Ledger)

한 항목 = 한 개의 "다시는 지적받지 않겠다"는 규칙.

```yaml
- id: L-001
  지적: useCallback을 습관적으로 붙임
  출처: PR #482 리뷰 코멘트
  검증법: 변경분에서 useCallback/useMemo 사용처 grep
  재발: 2회
```

`검증법`이 비어 있는 항목은 원장에 들어올 수 없다. `재발` 횟수가 4주차의 채점 지표가 된다.

### 역할 분리

| 역할 | 하는 일 | 왜 분리하나 |
|---|---|---|
| 구현자 | 설계서 → 코드 | |
| 리뷰어 | 원장 + 컨벤션 기준으로 판정 | **자기 코드를 자기가 승인하면 무조건 통과시킨다** |
| 검증자 | 빌드 · 타입 · 실제 화면 동작 | 코드를 읽는 것과 돌려보는 것은 다른 일 |

### 재사용할 기존 자산

새로 만드는 게 아니라 **하나의 루프로 묶는 것**이 과제다.

`/convention-review` · `/full-review` · `/pr` · `/ui-verify` · `/self-qa-checklist` · `/review-feedback-log`

## 4주 로드맵

각 주차의 성공 기준은 "사람 개입 횟수가 줄었는가".

| 주차 | 배울 개념 | 얹을 것 | 사람 개입 |
|---|---|---|---|
| 1 | 스킬 · 컨텍스트 | 설계서 → 구현 스킬 1개. 원장 v0 (수기 10항목) | 설계 + 매 단계 호출 |
| 2 | 에이전트 | 구현자 / 리뷰어 분리. 리뷰어는 원장으로만 판정 | 리뷰 결과 승인 |
| 3 | 오케스트레이터 · 루프 | 구현→리뷰→수정 루프 (상한 3회). 시각 검증 결합 | 시작 + 최종 확인 |
| 4 | 하네스 · 평가 | 재발률 채점. Draft PR까지 자동. 실패는 원장에 적립 | 시작만 |

버그 픽스 OS는 폐기하지 않고, 3주차에 **같은 골격을 재사용하는 두 번째 진입점**으로 검토한다.
원장과 리뷰어를 그대로 쓰고 앞단만 "재현"으로 바꾸면 된다.

## 현재 상태

- [x] OS 주제 선정 및 채택 근거 정리
- [x] 설계 초안 작성
- [ ] 원장 v0 (수기 10항목) 작성
- [ ] 설계서 → 구현 스킬 1개
- [ ] 구현자 / 리뷰어 에이전트 분리

## 열린 결정 사항

- [ ] 리뷰 원장의 소스를 어디서 가져올 것인가 (GitHub PR 코멘트 / Notion / 수기)
- [ ] 실습 대상 코드베이스를 무엇으로 할 것인가 (실제 업무 레포 / 별도 샘플)
- [ ] 설계서의 형식과 저장 위치
